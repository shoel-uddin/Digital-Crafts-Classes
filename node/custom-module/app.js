const coolfunction = require("./coolFunction")

const {api, superSecret} = require("./config") //{is the key} in the config file

const {function1, function2} = require("./moreFunctions")


console.log(api)
coolfunction()
function1()